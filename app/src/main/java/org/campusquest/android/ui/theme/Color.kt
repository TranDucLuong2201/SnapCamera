package org.campusquest.android.ui.theme

import androidx.compose.ui.graphics.Color

// Legacy colors (giữ nguyên để tương thích)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Modern Dark Theme Colors - Lấy cảm hứng từ Grok
val DarkPrimary = Color(0xFF3B82F6)        // Blue hiện đại
val DarkSecondary = Color(0xFF34C759)      // Xanh lá tươi
val DarkTertiary = Color(0xFFF59E0B)       // Cam ấm
val DarkBackground = Color(0xFF0F172A)     // Xám đậm
val DarkSurface = Color(0xFF1E293B)        // Xám đậm nhẹ
val DarkSurfaceVariant = Color(0xFF334155) // Xám sáng hơn
val DarkOnPrimary = Color(0xFFFFFFFF)      // Trắng
val DarkOnSecondary = Color(0xFF1A2E05)    // Xanh đậm
val DarkOnTertiary = Color(0xFF431407)     // Nâu đậm
val DarkOnBackground = Color(0xFFE2E8F0)   // Xám nhạt
val DarkOnSurface = Color(0xFFE2E8F0)      // Xám nhạt

// Modern Light Theme Colors - Lấy cảm hứng từ Grok
val LightPrimary = Color(0xFF3B82F6)       // Đồng bộ với DarkPrimary
val LightSecondary = Color(0xFF34C759)     // Đồng bộ với DarkSecondary
val LightTertiary = Color(0xFFF59E0B)      // Đồng bộ với DarkTertiary
val LightBackground = Color(0xFFF8FAFC)    // Trắng nhạt
val LightSurface = Color(0xFFFFFFFF)       // Trắng thuần
val LightSurfaceVariant = Color(0xFFE5E7EB) // Xám nhạt
val LightOnPrimary = Color(0xFFFFFFFF)     // Trắng
val LightOnSecondary = Color(0xFF1A2E05)   // Đồng bộ với DarkOnSecondary
val LightOnTertiary = Color(0xFF431407)    // Đồng bộ với DarkOnTertiary
val LightOnBackground = Color(0xFF1E293B)  // Xám đậm
val LightOnSurface = Color(0xFF1E293B)     // Xám đậm

// Semantic Colors - Tối ưu cho Grok style
val SuccessColor = Color(0xFF22C55E)       // Xanh lá tươi
val WarningColor = Color(0xFFF59E0B)       // Cam
val ErrorColor = Color(0xFFEF4444)         // Đỏ hiện đại
val InfoColor = Color(0xFF3B82F6)          // Đồng bộ với Primary

// Gradient Colors for Premium Feel - Lấy cảm hứng từ gradient của Grok
val GradientStart = Color(0xFF3B82F6)      // Blue
val GradientMiddle = Color(0xFF8B5CF6)     // Tím
val GradientEnd = Color(0xFFEC4899)        // Hồng

// Learning App Specific Colors - Tối ưu cho học tập
val VocabularyColor = Color(0xFF6D28D9)    // Tím hiện đại
val GrammarColor = Color(0xFF0EA5E9)       // Xanh dương sáng
val ListeningColor = Color(0xFFF87171)     // Đỏ san hô
val SpeakingColor = Color(0xFF2DD4BF)      // Xanh ngọc
val ReadingColor = Color(0xFFFACC15)       // Vàng sáng
val WritingColor = Color(0xFF4ADE80)       // Xanh lá

// AI Assistant Colors
val AIAccentColor = Color(0xFF14B8A6)      // Xanh ngọc
val AISecondaryColor = Color(0xFF8B5CF6)   // Tím

// Social Features Colors
val FriendColor = Color(0xFF22C55E)        // Đồng bộ với SuccessColor
val LeaderboardColor = Color(0xFFF59E0B)   // Đồng bộ với Tertiary
val AchievementColor = Color(0xFFD97706)   // Đổi thành vàng cam đậm, gần với Grok style